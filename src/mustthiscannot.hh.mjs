const MustThisCannot = () => hiphop {
  fork {
     loop {
	let m = must00.nowval;
	if (m.size === 1)
	   emit cannot00(digits.difference(m));
	yield;
     }
  } par {
     /**&vellip;**/
  } par {
     loop {
	let m = must88.nowval;
	if (m.size === 1)
	   emit cannot88(digits.difference(m));
	yield;
     }
  }
